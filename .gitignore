# Dependencies
node_modules/
# Ignore npm lock file if accidentally created
package-lock.json
# Keep pnpm-lock.yaml for reproducible builds

# Production
dist/
trailblazer-analytics-dist.zip

# Archive
archive/

# Env
.env

# Build metadata (generated during builds)
build-metadata.json
scripts/build-metadata.json

# Claude workspace
.claude/
